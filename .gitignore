# --- 官方美术：版权归库洛游戏，本仓库只留「取回用的来源表」--------------------
# 这些文件由插件（lib/index.js）在安装时从库洛官方站点 / 资源 CDN 本地取回。
# 来源与 sha256 锚点见 tools/asset-sources.json。
#
# **例外**：skin/assets/jinhsi-quiet.mp4 —— v1.4.0 起主题的背景是这段随包分发的
# 动态壁纸（Wallpaper Engine 创意工坊 item 3606711102 的导出件），它必须入库，
# 否则别的机器装上皮肤就没有底图。来源与授权声明见 NOTICE，别加进这条忽略规则。
skin/assets/*.webp
skin/assets/*.png
skin/assets/*.jpg

# --- 本机验证产物：含官方美术或第三方壁纸，不得进入仓库 ----------------------
# WE 适配对照图里既有官方立绘，也有 Wallpaper Engine 创意工坊作者的壁纸。
we-adaptation.jpg

# --- 预览图：随仓库分发（manifest 要求 preview 存在）。
#     v1.4.0 起背景是随包分发的动态壁纸，所以仓库里的 light/dark 预览图也用真实底图渲染，
#     让店铺里看到的就是实际效果；需要「不含版权素材」的版本时跑
#     tools/make-preview.mjs --placeholder 覆盖生成即可，那一版不要提交。

# --- 运行时产物 -------------------------------------------------------------
*.log
*.tmp
_*
node_modules/

# --- 本机 DSH 相关：绝不入库 ------------------------------------------------
.credentials.yaml
.env
.env.*
*.token
